{
  "name": "cdn_headers",
  "version": "0.0.1",
  "description": "FastEdge CDN application for integration tests - headers manipulation",
  "type": "module",
  "main": "assembly/headers-change.ts",
  "scripts": {
    "build": "npm-run-all -s clean build:all copy:all clean",
    "build:all": "npm-run-all -p build:headers-change build:headers",
    "build:headers-change": "asc assembly/headers-change.ts --target release --outFile build/headers-change.wasm --textFile build/headers-change.wat --sourceMap --optimize",
    "build:headers": "asc assembly/headers.ts --target release --outFile build/headers.wasm --textFile build/headers.wat --sourceMap --optimize",
    "copy:all": "npm-run-all -p copy:headers-change copy:headers",
    "copy:headers-change": "npx cross-env NAME=headers-change npm run mv:file",
    "copy:headers": "npx cross-env NAME=headers npm run mv:file",
    "mv:file": "mv build/$NAME.wasm ../../../../wasm/cdn-apps/as/headers/$NAME.wasm",
    "clean": "rm -rf build && mkdir -p build"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "ISC"
}
